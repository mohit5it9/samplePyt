jobs:
  - name: gke-man-1
    type: manifest
    steps:
      - GET: gke-nginx
      - GET: image-opts
      - GET: trigger-gke-man

  - name: gke-beta-deploy-1
    type: gkeDeploy
    options:
      deployMethod: upgrade
    steps:
      - GET: gke-man-1
      - GET: gke-cluster-1
