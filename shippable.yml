
# Language setting
language: python

# Version number
python:
  - 2.7
  - 2.8

build:
  ci:
    - python --version

integrations:
  notifications:
      - integrationName: pranay-hipchat
        type: hipchat
        recipients:
          - '#Pillar%20of%20Autumn'
          - '@PranayPareek'
          - '#shippable'
        on_start: always
        on_success: always
        on_failure: always
        on_pull_request: always
