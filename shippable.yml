
# Language setting
language: python

# Version number
python:
  - 2.7

env:
  global:
    - FOO="bar"
    - FIZZ="buzz"
  matrix:
    - FOO1="bar1"
    - FOO2="bar2"

build:
  ci:
    - python --version
