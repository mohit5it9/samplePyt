
# Language setting
language: python

# Version number - master
python:
  - 2.6

build:
  ci:
    - python --version

integrations:
    notifications:
        - integrationName: pranay-slack
          type: slack
          recipients:
            - '#test_interns'
