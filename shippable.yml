
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7
  - 3.2

build:
  ci:
    - python --version
    - if [ "$SHIPPABLE_PYTHON_VERSION" == "3.2" ]; then foobar; fi

integrations:
    notifications:
        - integrationName: email
          type: email
          recipients:
            - pranay@shippable.com
          on_start: always
          on_success: always
          on_failure: always
          on_pull_request: always
