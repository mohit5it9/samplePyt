
# Language setting
language: python

# Version number
python:
  - 3.2

build:
  ci:
    - python --version
    - if [ "$SHIPPABLE_PYTHON_VERSION" == "3.2" ]; then foobar; fi
    
integrations:
  notifications:
    - integrationName: slack
      type: slack
      recipients:
        - "#guilty_spark"
        - #test_interns
      on_success: never
      on_failure: always
